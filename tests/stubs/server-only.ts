// Test stub for the `server-only` guard package. In production this module
// throws if imported into a client bundle; under Vitest we replace it with a
// harmless no-op so server-marked logic modules can be unit tested directly.
export {}
